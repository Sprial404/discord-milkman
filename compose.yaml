services:
  server:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: unless-stopped
    develop:
      watch:
        - action: rebuild
          path: .
          ignore:
            - ./data
